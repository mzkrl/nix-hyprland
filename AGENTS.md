# Repo notes

- This is a NixOS flake (Caelestia Shell rice + Home Manager).
- Always work on the branch: `caelestia-improved`.
- Deploy: `nh os switch /home/juang/Pictures/hyprland/hyprland-claude`
- Wallpaper daemon: Caelestia Shell native background (`background.enabled` in `home/default.nix`), set at login with `caelestia wallpaper -f` in `configs/hypr/hyprland.conf`; wallpaper at `./wallpaper.png` (deployed to `~/Pictures/Wallpapers/`).